apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "beer-micro-service-jms.fullname" . }}-test-connection"
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "beer-micro-service-jms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: test-jms-connection
      image: apache/activemq-artemis:2.41.0
      command: [ '/bin/sh', '-c' ]
      args:
        - |
          echo "### Testing Artemis JMS connection : {{ include "beer-micro-service-jms.fullname" . }}"
          artemis queue stat --url tcp://{{ include "beer-micro-service-jms.fullname" . }}:{{ .Values.service.broker.port }} --user $ARTEMIS_USERNAME --password $ARTEMIS_PASSWORD --silent
          if [ $? -eq 0 ]; then
            echo "Successfully connected to Artemis JMS broker"
          else
            echo "Failed to connect to Artemis JMS broker"
          fi
          echo "Exit code: $?"

      envFrom:
        - secretRef:
            name: {{ include "beer-micro-service-jms.fullname" . }}-secrets
  restartPolicy: Never
